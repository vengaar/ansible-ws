#
# {{ ansible_managed }}
#
---
parameters:
  action:
    desc: ""
    choices:
      - kill
    default: "kill"
examples:
  - desc: "To get information about agent"
    url: "{{ url }}"
...