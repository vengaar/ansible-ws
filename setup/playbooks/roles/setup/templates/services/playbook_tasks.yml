#
# {{ ansible_managed }}
#
{% set playbook = "/home/" + wsgi_user + "/ansible-ws/tests/data/playbooks/tags.yml" %}
---
cache:
  resource: playbook
  type: tags
parameters:
  playbook:
    desc: "The playbook from gather tasks"
    attributes:
      - required
  format:
    desc: "The output format of json results"
    choices:
      - "sui"
      - "raw"
    default: "sui"
  cache:
    desc: "To manage cache"
    choices:
      - "use"
      - "pass"
      - "flush"
    default: "use"
examples:
  - desc: "To get tasks for playbook {{ playbook }}"
    url: "{{ url }}?playbook={{ playbook }}"
...